# 
# Configuration under control of Puppet!
#
storm.zookeeper.servers:
<% zookeeper_servers.each do |zk| -%>
  - "<%= zk %>"
<% end -%>
nimbus.host: "<%= @nimbus_host %>"

worker.childopts: "-Xmx<%= worker_mem %>m"
storm.local.dir: "/var/cache/storm"

ui.port: <%= ui_port %>

java.library.path: "/usr/local/lib:/opt/local/lib:/usr/lib64:/usr/lib:/usr/local/lib64:/usr/lib/hadoop/lib/native"

supervisor.slots.ports:
<% supervisor_slots.each do |slot| -%>
  - <%= slot %>
<% end -%>

storm.zookeeper.session.timeout: 60000
storm.zookeeper.connection.timeout: 30000
storm.zookeeper.retry.times: 10
storm.zookeeper.retry.interval: 2000
storm.zookeeper.retry.intervalceiling.millis: 60000